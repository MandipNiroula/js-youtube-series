/*


const accountId = 130110  // once declared then cannot be changed
let accountEmail = "mandi@gmail.com"  // it can be changed
var accountPassword = "444"  //it can be changed , this is not used due to scope problem
accountCity = "itahari"  // this is not used

// accountId = 888 ,  this will produce an error

console.log(accountId);

//shows te variable in tabuler manner with index (start from 0).
console.table([accountId, accountEmail, accountPassword,accountCity])


